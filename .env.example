# The only two environment variables this codebase reads.
# Both are used solely by the contact form (src/app/api/contact/route.ts).
# Everything else on the site is static — no database, no auth.

RESEND_API_KEY=
CONTACT_EMAIL=
